import {BindingKey} from '@loopback/context';
export namespace ArgoComputeApiBindings {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  export const CONFIG = BindingKey.create<any>('ArgoCompute.config');
}
