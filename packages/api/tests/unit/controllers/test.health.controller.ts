// import { expect } from '@loopback/testlab';
// import { HealthController } from '../../../controllers';
// import { Request } from '@loopback/rest';

// describe('Health Controller (unit)', () => {
// 	it('retrieves details of a product', () => {
// 		const req = { url: 'test' };
// 		const controller = new HealthController(req as Request);

// 		const details = controller.ping();

// 		expect(details).to.containEql({
// 			greeting: 'Compute API is healthy',
// 			url: 'test',
// 			headers: {},
// 		});
// 	});
// });
