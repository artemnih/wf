import {BindingKey} from '@loopback/context';
export namespace SlurmComputeApiBindings {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  export const CONFIG = BindingKey.create<any>('SlurmCompute.config');
}
